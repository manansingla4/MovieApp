<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group
        android:id="@+id/list_screen_select"
        android:checkableBehavior="single">
        <item
            android:id="@+id/nav_movies"
            android:icon="@drawable/ic_movie"
            android:title="@string/movies" />
        <item
            android:id="@+id/nav_tv_shows"
            android:icon="@drawable/ic_tv"
            android:title="@string/tv_shows" />
    </group>
    <item
        android:id="@+id/search_drawer_item"
        android:icon="@drawable/ic_search"
        android:title="@string/search" />
    <item
        android:id="@+id/favourite_drawer_item"
        android:icon="@drawable/ic_favorite"
        android:title="@string/favourite" />
</menu>